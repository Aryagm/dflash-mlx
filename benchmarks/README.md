# Benchmarks

`summary.csv` contains the public benchmark numbers used by the README charts.

`metrics_history.csv` is the curated run log with command-generated benchmark rows and full run metadata. Chat-backfilled reconstruction rows are intentionally excluded.
