# Benchmarks

`summary.csv` contains the public benchmark numbers used by the README charts.

`metrics_history.csv` is intentionally ignored. The benchmark CLIs append detailed local run metadata there, including git status and experimental tags, so it is useful for local iteration but too noisy for the public repo.
